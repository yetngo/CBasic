#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int main(int argc, char const *argv[])
{
    int n,i,s;
    printf("n="); scanf("%d",&n);
    s==0;
    for (i=1;i<=n;i++)
    {
        int a;
        printf("so %d = ",i); scanf("%d",&a);
        s=s+a;
    }
    // break point
    printf("tong= %d", s);
    getch();
    return 0;
}